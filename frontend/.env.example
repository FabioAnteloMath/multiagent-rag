# Frontend env vars (set these in the Vercel dashboard or via `vercel env`)
#
# Production example (Fly.io backend):
#   NEXT_PUBLIC_API_URL=https://multiagent-rag.fly.dev/api
#
# Local dev (default fallback already in lib/api.ts):
#   NEXT_PUBLIC_API_URL=http://localhost:8011/api
